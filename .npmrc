# Hoist nothing implicitly. A package that uses a dependency it does not declare
# works by accident until the day the tree shifts; in a workspace where three
# apps share packages/contracts, that day arrives during a release.
strict-peer-dependencies=false
auto-install-peers=true
link-workspace-packages=true
