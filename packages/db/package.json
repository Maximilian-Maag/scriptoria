{
  "name": "@scriptoria/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"no tests in @scriptoria/db\"",
    "db:generate": "drizzle-kit generate",
    "db:check": "tsx src/checkSchemaDrift.ts",
    "db:migrate": "tsx src/migrate.ts",
    "db:push": "drizzle-kit push --force",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@scriptoria/config": "workspace:*",
    "@scriptoria/contracts": "workspace:*",
    "@scriptoria/core": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
